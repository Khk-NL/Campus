{
  "name": "@campus/campus-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Campus SDK v1 接口面 —— 插件作者侧的调用契约（§13-Phase 4）",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@campus/models": "workspace:*",
    "@campus/plugin-runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
